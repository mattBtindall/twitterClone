{% if user is defined %}
    <img src="{{ asset(profiles ~ user.avatar) }}" alt="dogs" style="width: 64px;" class="{{ iconClasses }} rounded-circle bg-dark p-2">
{% else %}
    {{ include ('shared/_icon.html.twig', { name : 'question-circle-fill', size: 32, classes: iconClasses })}}
{% endif %}
