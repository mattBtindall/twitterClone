{% extends 'base.html.twig' %}

{% block breadcrumbs %}
    → {{ pageTitle }}
{% endblock %}

{% block content %}
    <form class="px-3 py-4" action="{{ path('app_login') }}" method="POST">
        <div class="row">
            <div class="col-lg-6">
                <div class="form-group">
                    <label class="form-label">Email</label>
                    <input class="form-control form-control-lg mb-2" type="text" name="_username" value="{{ lastUsername }}" />
                </div>
            </div>
            <div class="col-lg-6">
                <div class="form-group">
                    <label class="form-label">Password</label>
                    <input class="form-control form-control-lg mb-2" type="password" name="_password" />
                </div>
            </div>
        </div>

        <input type="hidden" name="_target_path" value="{{ path('app_posts') }}">
        <div class="row">
            <div class="col-lg-6">
                <input class="btn btn-primary w-100" type="submit" value="login">
            </div>
        </div>
    </form>
{% endblock %}
